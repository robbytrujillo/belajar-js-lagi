// Conditional Operator
// Jalan pintas untuk if dan else

// 1 tanpa conditional operator
const user = "Admin";
let result = "";

if (user === "Admin") {
  result = "Welcome, admin";
} else {
  result = "User not found";
}

console.log(result); // Welcome, admin
